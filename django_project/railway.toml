[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn adubiaro_portal.wsgi --workers 2 --bind 0.0.0.0:$PORT"
healthcheckPath = "/accounts/login/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
